<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Create Post</title>
<script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-white flex items-center justify-center min-h-screen">
<form 
action="/user/update" 
method="POST" 
enctype="multipart/form-data"
class="w-full max-w-md p-5 space-y-5">
<h2 class="text-4xl font-semibold text-center">Update Profile</h2>
<div class="w-full">


 <div class="w-full max-w-sm mx-auto mt-10">
    <label class="block text-sm font-medium text-gray-200 mb-1">
    Profile Image
  </label>
    <input 
type="file" 
name="picture"
accept="image/*,video/*"
class="w-full bg-zinc-900 p-3 rounded-lg border border-zinc-700 mb-4"/>

  <label class="block text-sm font-medium text-gray-200 mb-1">
    Username
  </label>

  <input 
    type="text"
    name="username"
    value="<%= user.username %>"
    class="w-full px-3 py-2 bg-neutral-900 border border-neutral-700 rounded-md 
           text-white placeholder-gray-500 
           focus:outline-none focus:ring-1 focus:ring-gray-400 focus:border-gray-400
           transition duration-200 mb-4"
  />
  <label class="block text-sm font-medium text-gray-200 mb-1">
    name
  </label>
  <input 
    type="text"
    name="name"
    value="<%= user.name %>"
    class="w-full px-3 py-2 bg-neutral-900 border border-neutral-700 rounded-md 
           text-white placeholder-gray-500 
           focus:outline-none focus:ring-1 focus:ring-gray-400 focus:border-gray-400
           transition duration-200"
  />
  <div class="mt-4">
      <label class="block text-sm font-medium text-gray-200 mb-1">Bio</label>
      <textarea 
       value="<%= user.bio %>"
      rows="3"
      placeholder="Write a caption..."
      class="w-full bg-zinc-900 p-3 rounded-lg border border-zinc-700 outline-none resize-none"></textarea>
    </div>  
</div>

</div>
<input 
type="submit"
class="w-full bg-blue-600 hover:bg-blue-700 transition py-3 rounded-lg font-semibold" value="Update Profile">
</form>
</body>
</html>
